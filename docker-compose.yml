version: '3'

services:
  import:
    image: import-script
    build: .

  db:
    image: postgres:14
    environment:
      - POSTGRES_DB=mesh
      - POSTGRES_PASSWORD=meshterms
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  db-data:
